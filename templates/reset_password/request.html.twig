{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}

    <main role="main">

		<section class="jumbotron text-center bg-light pt-4 pb-4">
			<div class="container">
				<h1 class="jumbotron-heading"> Mot de passe oublie ? </h1>
			</div>
		</section>

        <div class="album py-3">
            <div class="container col-md-4">

                <p>
                    Entrez simplement l’adresse e-mail avec laquelle vous vous 
                    êtes inscrit·e et nous vous enverrons un lien pour réinitialiser 
                    votre mot de passe.
                </p>

                <div class="mb-6">
                    {{ form_start(requestForm) }}
                    {{ form_row(requestForm.email) }}
                    <button class="btn btn-sm btn-primary"> Envoyer le lien de réinitialisation de mot de passe </button>
                    {{ form_end(requestForm) }}
                </div>

            </div>
        </div>

    </main>
{% endblock %}
