
{{ form_start(form) }}
	{{ form_row(form.nom) }}
	{{ form_row(form.parent) }}

	{# <button class="btn">{{ button_label|default('Save') }}</button> #}
	<button class="btn btn-primary mt-2 w-100 mb-3" type="submit" >Valider</button>

{{ form_rest(form) }}
{{ form_end(form) }}
		